#!/usr/bin/env python3
"""
Ralph Server - Start the Ralph API gateway.

Usage:
    ralph-server
    ralph-server --port 8765
    ralph-server --host 0.0.0.0 --port 8080 --reload
"""

import sys
from pathlib import Path

# Add parent directory to path for imports
sys.path.insert(0, str(Path(__file__).parent.parent))

from src.ralph_gateway import main

if __name__ == '__main__':
    main()
